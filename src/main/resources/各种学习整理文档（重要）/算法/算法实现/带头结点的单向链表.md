​												带头结点的单向链表

```

public class SingleLinkedListDemo {
    public static void main(String[] args) {

    SingleLinkedList linkedList = new SingleLinkedList();
    HeroNode node1=new HeroNode(1,"宋江","及时雨");
    HeroNode node2=new HeroNode(2,"卢俊义","玉麒麟");
    HeroNode node3=new HeroNode(3,"吴用","智多星");
    HeroNode node4=new HeroNode(4,"林冲","豹子头");
    HeroNode node5=new HeroNode(3,"吴大用","文曲星");

    /*linkedList.addNodeAtLast(node1);
    linkedList.addNodeAtLast(node4);
    linkedList.addNodeAtLast(node3);
    linkedList.addNodeAtLast(node5);*/
     //linkedList.addNodeAtLast(node3);
        linkedList.addNodeWithSort(node4);
        linkedList.addNodeWithSort(node3);
        linkedList.addNodeWithSort(node1);
        linkedList.addNodeWithSort(node2);

        System.out.println("显示链表数据：");
        linkedList.addNodeWithSort(node1);
        linkedList.list();

        System.out.println("显示链表数据：");
        linkedList.updateNode(node5);
        linkedList.list();


        System.out.println("显示链表数据：");
        linkedList.deleteNode(2);
        linkedList.list();
    }


}

//实现链表增删改查功能
class SingleLinkedList {
    private HeroNode head = new HeroNode(0, "", "");

    public void addNodeAtLast(HeroNode heroNode) {
        HeroNode temp = head;

        while (true) {
            if (temp.next == null) break;

            temp = temp.next;

        }

        temp.next = heroNode;
    }


    public void addNodeWithSort(HeroNode heroNode) {
        HeroNode temp = head;

        boolean flag = false;
        while (true) {
            if (temp.next == null) {
                flag = true;
                break;
            }

            if (temp.next.no > heroNode.no) {
                flag = true;
                break;
            } else if (temp.next.no == heroNode.no) {
                break;
            }
            temp=temp.next;
        }

        if (flag){
            heroNode.next=temp.next;
            temp.next=heroNode;
        }else {
            System.out.printf("节点号：%d已经存在",heroNode.no);
        }
    }

    public void updateNode(HeroNode node){
        //假设no相同，两个节点相同
        HeroNode temp = head;
        boolean flag= false;
        while (true){
            if (temp.next==null) break;

            if (temp.next.no==node.no){
                flag=true;
                break;
            }else {
                temp=temp.next;
            }


        }

        if (flag){
            temp.next.name=node.name;
            temp.next.nickName=node.nickName;
        }else {
            System.out.printf("找不到no为:%d的英雄",node.no);
        }
    }

    public void deleteNode(int no){
        HeroNode temp=head;
        boolean flag = false;
        while (true){
            if (temp.next==null)break;

            if (temp.next.no==no){
                flag=true;
                break;
            }else {
                temp=temp.next;
            }
        }

        if (flag){
            temp.next=temp.next.next;
        }else {
            System.out.printf("没找到英雄%d号",no);
        }

    }


    public void list(){
        HeroNode temp = head;

        while (true){
            if (temp.next==null) break;

            temp=temp.next;

            System.out.println("temp = " + temp);
        }
    }


}


class HeroNode {
    public int no;

    public String name;

    public String nickName;

    //一会关注下为什么不能改为private
    public HeroNode next;

    public HeroNode(int no, String name, String nickName) {
        this.no = no;
        this.name = name;
        this.nickName = nickName;
    }

    @Override
    public String toString() {
        return "HeroNode{" +
                "no=" + no +
                ", name='" + name + '\'' +
                ", nickName='" + nickName + '\'' +
                '}';
    }

  /*  public HeroNode getNext() {
        return next;
    }

    public void setNext(HeroNode next) {
        this.next = next;
    }*/
}
```